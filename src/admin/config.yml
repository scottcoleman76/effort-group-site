backend:
  name: git-gateway
  branch: main

publish_mode: simple
media_folder: "src/articles-media"
public_folder: "/articles-media"

collections:
  - name: "articles"
    label: "Articles"
    label_singular: "Article"
    folder: "src/content/articles"
    create: true
    format: "json"
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{title}} — {{author}} — {{date}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - label: "Author"
        name: "author"
        widget: "select"
        options: ["Angie Coleman", "Scott Coleman", "Emma Coleman"]
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Cover Image", name: "image", widget: "image", required: false }
      - label: "Categories (pick 2-3 core topics — each becomes its own indexable page)"
        name: "category"
        widget: "list"
        hint: "Type your categories separated by commas, e.g. Leadership, Coaching, Strategy — note: typing a space directly into this box doesn't work due to an editor bug, so type in Notes/Word first, then paste it in here. Reuse existing ones (Strategy, AI, Engagement, Teams, Leadership, Economic, Coaching, Culture) where they fit, or add new ones. Keep this to 2-3 per article for the strongest SEO signal."
      - label: "Tags (optional, additional keywords)"
        name: "tags"
        widget: "list"
        required: false
        hint: "Type your tags separated by commas, e.g. Remote Work, Q3 Planning, Retention — note: typing a space directly into this box doesn't work due to an editor bug, so type in Notes/Word first, then paste it in here."
      - { label: "Excerpt (short summary shown on the articles list)", name: "excerpt", widget: "text" }
      - { label: "Body", name: "body", widget: "markdown" }
      - label: "Sources"
        name: "sources"
        widget: "list"
        required: false
        field: { label: "Source (name and/or link)", name: "source", widget: "string" }
